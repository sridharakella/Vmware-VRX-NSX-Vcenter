apiVersion: apps/v1
kind: Deployment
metadata:
  name: app2-deployment
  namespace: tenant-b
spec:
  replicas: 1
  selector:
    matchLabels:
      app: app2
  template:
    metadata:
      labels:
        app: app2
    spec:
      containers:
      - name: app
        image: gcr.io/google-samples/hello-app:2.0
        ports:
        - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: app2-svc
  namespace: tenant-b
spec:
  selector:
    app: app2
  ports:
  - port: 80
    targetPort: 8080
